<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.mohang.mapper.AccountMapper">
	<insert id="memberJoin">
		insert into ACCOUNT values(
								   account_seq.nextval ,#{accountId}, #{accountPassword}, 
								   #{accountName}, #{accountEmail}, #{accountAddress},
								   sysdate, 0, #{accountInterest}, sysdate, 
								   #{accountGender}, #{accountPhonenumber}, #{accountAddress2}, #{accountAddress3})
	</insert>

	<select id="idCheck" resultType="int">
  	
  		select count(*) from ACCOUNT where account_Id = #{account_Id}
  	
  	</select>

</mapper>